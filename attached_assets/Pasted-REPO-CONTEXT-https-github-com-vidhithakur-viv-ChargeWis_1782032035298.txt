REPO CONTEXT: https://github.com/vidhithakur-viv/ChargeWise — already imported into this Replit.

The repo currently has a messy structure:
- backend/, data/, gis/ = backend (do not touch their logic)
- frontend/src/ = an earlier, mostly broken frontend attempt
- stitch_chargewise_ai_infrastructure_platform (1) through (5) = a newer frontend, accidentally uploaded as 5 duplicate numbered folders instead of merging into one (this happens when multiple folders with the same name are dragged into GitHub's web uploader)

STEP 1 — INVESTIGATE AND REPORT (do this first, don't change anything yet):
Open all 5 stitch_chargewise_ai_infrastructure_platform folders and tell me:
- Are they exact duplicates of each other, or do they contain different pages/sections of the same site?
- What framework is it (plain HTML/CSS/JS, React, Vite, etc.) and is it a complete, runnable frontend?
- Does it already implement the 5 features (EV Opportunity Heatmap, Charging Desert Detection, Recommendation Engine, ROI Calculator, AI Copilot) or just the visual design?
- Should we use this Stitch frontend as the final one (replacing frontend/src) or merge pieces of both?
Give me this summary before proceeding to Step 2.

STEP 2 — CLEAN UP (after I confirm, or proceed automatically if the answer is obviously "these are identical duplicates"):
- Merge the 5 numbered folders into a single clean folder (use frontend/src as the target location)
- Delete the duplicate numbered folders once merged
- Delete the old broken frontend/src content if the Stitch version is replacing it — but tell me explicitly before deleting anything in case some of it is still needed

STEP 3 — INTEGRATE WITH BACKEND:
- Explore backend/ to find existing Flask API endpoints (routes, params, response shape) for each of the 5 features
- Wire up the consolidated frontend to call these real endpoints — replace any placeholder/mock data with real API calls
- Every button, nav link, and toggle must trigger a real action: clicking a feature card on the landing page goes to the dashboard with that layer active; toggles call the right endpoint and update the Leaflet map; ROI calculator panel computes from real cell data
- If an endpoint doesn't exist for some feature, tell me which one before stubbing or faking data

STEP 4 — ANIMATIONS:
- Tab/nav switching and panel open/close should fade + slide (translateX + opacity, ~250-300ms, ease-in-out) rather than instant jumps
- Apply consistently across landing page nav, dashboard layer panels, and the ROI calculator panel

STEP 5 — VERIFY:
- Confirm the app runs with Replit's Run button (single command for both backend + frontend, use a Procfile or concurrently if needed)
- Go through every button/toggle and confirm each does something real (check network tab for actual API calls, not just "no error thrown")
- Test on both desktop and mobile widths

CONSTRAINTS:
- Do not modify backend/, data/, or gis/ logic — only call their existing endpoints
- Keep whatever dark/cyan EV-tech design language is already in the Stitch frontend
- Ask before deleting any folder you're not sure about